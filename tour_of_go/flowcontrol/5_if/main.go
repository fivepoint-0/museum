package main

import (
	"fmt"
)

func main() {
	// like for-loops, parenth. not required. but the braces are required.
	if 1 < 10 {
		fmt.Println("Math still works")
	} else {
		fmt.Println("Time to redo all of arithmetic")
	}
}
